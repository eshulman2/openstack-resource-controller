---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  # We should be able to delete the import dependencies
  - command: kubectl delete dnszone.openstack.k-orc.cloud dnsrecordset-import-dependency
    namespaced: true
